

let myState = {
    user: {}
}

const authReducer = (state = myState, action) =>{
    switch (action.type) {
        // case value:
            
        //     break;
    
        default:
            return state;
    }

}

export default authReducer;